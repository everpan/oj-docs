<!-- 由 scripts/sync-docs.mjs 于 2026-09-12 从 `oj-module:docs/prd/oj-fullstack-tutorial.md` 生成，请勿直接编辑；改源文件后运行 `npm run sync` -->

---
title: 6. 联调与验证
generated: 2026-09-12
---

<p class="gen-note">generated: 2026-09-12 · 本页由脚本从 oj-module:docs/prd/oj-fullstack-tutorial.md 同步生成，修改请改源文件后运行 npm run sync。</p>

[← 返回《全栈实战演练》](./index.md)

# 6. 联调与验证

1. `pnpm dev` 起来，登录 `admin / 123456`；
2. 左侧出现「图书管理」，点进去应看到 3 条种子数据；
3. 输入关键词搜索 → Network 里 `GET /api/books/list?keyword=…` 返回 200；
4. 新增一本 → `POST /api/books/create` 200，列表刷新；
5. 加错必填项 → 后端返回 `400`，前端弹 `ContractApiError.msg`。

**契约维度的自查**：

```bash
pnpm exec ojm api --check
pnpm exec ojm info          # 版本矩阵 + 模块清单
```

---

